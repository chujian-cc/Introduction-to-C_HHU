#include <stdio.h>

int main() {
    for(int a = 0; a <= 9; a++)
        for(int b = 0; b <= 9; b++)
            for(int c = 0; c <= 9; c++)
                for(int d = 0; d <= 9; d++)
                    for(int e = 0; e <= 9; e++)
                        if(a != b && a != c && a != d && a != e && b != c && b != d && b != e && c != d && c != e && d != e && (a * 1000 + b * 100 + c * 10 + d) * e == 1000 * c + 100 * b + 10 * a + e)
                            printf("%d %d %d %d %d\n", a, b, c, d, e);
    return 0;
}
